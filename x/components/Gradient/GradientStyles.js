import { StyleSheet } from 'react-native';

export default (theme) => {
    let GradientStyles = {
        defaultTypeGradient: {
            height: '100%',
            width: '100%',
        },
    };

    return StyleSheet.create(GradientStyles);
}
