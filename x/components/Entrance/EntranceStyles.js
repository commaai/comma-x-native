import { StyleSheet } from 'react-native';

export default (theme) => {
    let EntranceStyles = {
        defaultTypeLine: {
            width: '100%',
        },
    };

    return StyleSheet.create(EntranceStyles);
}
